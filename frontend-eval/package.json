{
  "name": "42-challenge-frontend-eval",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright-based frontend evaluator for the 42-Challenge",
  "scripts": {
    "test": "playwright test",
    "install:browsers": "playwright install chromium --with-deps"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@lhci/cli": "^0.14.0",
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
